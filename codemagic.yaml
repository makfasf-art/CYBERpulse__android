workflows:
  android-release:
    name: Build
    environment:
      flutter: stable
    triggering:
      events:
        - push
    scripts:
      - name: Build
        script: flutter build apk --release
    artifacts:
      - build/app/outputs/apk/release/app-release.apkq
